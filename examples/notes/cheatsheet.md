# Quick Reference

## Git
```bash
git stash push -m "wip"
git stash pop
git rebase -i HEAD~3
```

## Docker
```bash
docker compose up -d
docker logs -f container
docker exec -it container sh
```

## Shortcuts
| Action | Key |
|--------|-----|
| Save | ⌘S |
| Find | ⌘F |
| Terminal | ⌘` |
